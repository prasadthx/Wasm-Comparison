@_cdecl("find_primes")
func find_primes(_ number: Int) -> Int {
	var primeNumbers : Int = 0;
    var languages : [String] = ["Swift", "Java", "Go", "JavaScript"]
    for language in languages {
        print(language)
    }
    // return primeNumbers;
    return number
}

